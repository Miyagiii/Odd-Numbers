import odd,random
x = random.randint(0,1000) # Gets a number to put into the algorithm
print("Finding the odd numbers below: "+str(x)) # Tells the user what number it's looking for
print(odd.findOdd(x)) # Finds x
